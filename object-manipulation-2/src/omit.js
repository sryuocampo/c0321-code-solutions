/* exported omit */
function omit(source, keys) {
  var results = {};
  for (var i = 0; i < keys.length)
}

// source = {foo: 1, bar: 2, baz: 3}
// keys = ['foo', 'baz']
// results = {}
// results.bar = source.bar
//return {bar: 2}
